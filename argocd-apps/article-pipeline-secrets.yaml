# @format
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: article-pipeline-secrets
  annotations:
    kubernetes.io/description: "ExternalSecrets for article-pipeline namespace"
    argocd.argoproj.io/sync-wave: "2"
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:Nelson-Lamounier/kubernetes-bootstrap.git
    targetRevision: main
    path: charts/article-pipeline/external-secrets
  destination:
    server: https://kubernetes.default.svc
    namespace: article-pipeline
  syncPolicy:
    automated: { prune: true, selfHeal: true, allowEmpty: false }
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - PruneLast=true
    retry:
      limit: 3
      backoff: { duration: 5s, factor: 2, maxDuration: 3m }
