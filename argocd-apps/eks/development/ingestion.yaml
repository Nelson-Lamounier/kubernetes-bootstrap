# @format
# ArgoCD Application: ingestion (EKS — Plan 5b Phase 6)
#
# Job-only chart: namespace + SA + RBAC for RSS ingestion Jobs created
# by admin-api.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingestion-eks-development
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
    kubernetes.io/description: "ingestion namespace + RBAC on EKS (Plan 5b)"
  labels:
    app.kubernetes.io/part-of: argocd
    cluster: eks
    environment: development
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Nelson-Lamounier/kubernetes-bootstrap.git
    targetRevision: main
    path: charts/ingestion/chart
    helm:
      releaseName: ingestion
  destination:
    server: https://kubernetes.default.svc
    namespace: ingestion
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
