# actionlint configuration
#
# Declares custom self-hosted runner labels so actionlint doesn't flag them
# as unknown. The k8s-runner label maps to the in-cluster ARC pods deployed
# from charts/arc-runners/.
self-hosted-runner:
  labels:
    - k8s
    - k8s-runner
