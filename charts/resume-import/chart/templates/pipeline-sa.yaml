# @format
# ServiceAccount mounted by resume-import-processor Job pods.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.pipelineServiceAccount.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: resume-import
