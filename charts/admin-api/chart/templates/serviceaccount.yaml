# @format
# admin-api ServiceAccount.
# Used as the RoleBinding subject for cross-namespace Job creation in the
# ingestion namespace (see kubernetes-bootstrap/charts/ingestion/chart).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: admin-api
