# @format
apiVersion: v2
name: ingestion
description: Ingestion namespace infra — ServiceAccount + RBAC. Jobs are created programmatically by admin-api.
type: application
version: 0.1.0
appVersion: "1.0.0"
