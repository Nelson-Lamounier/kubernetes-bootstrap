# ---------------------------------------------------------------
# Preview Service — routes traffic to the preview (blue/green) ReplicaSet
#
# Argo Rollouts requires a separate "preview" Service for blue/green
# deployments. The new version's pods receive traffic through this
# Service during pre-promotion analysis.
#
# During rollout: preview pods ← nextjs-preview Service
#                 active pods  ← nextjs Service (unchanged)
# After promotion: all pods    ← nextjs Service
# ---------------------------------------------------------------
apiVersion: v1
kind: Service
metadata:
  name: nextjs-preview
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nextjs.fullLabels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nextjs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
