apiVersion: v1
kind: ResourceQuota
metadata:
  name: nextjs-quota
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nextjs.labels" . | nindent 4 }}
spec:
  hard:
    {{- toYaml .Values.resourceQuota.hard | nindent 4 }}
